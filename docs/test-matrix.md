# the-game Test Matrix

| Scenario | Input or precondition | Expected outcome | Type | Automated |
|---|---|---|---|---|
| Electron boot | New character from main menu | Gameplay state, visible canvas, loaded assets | E2E | Yes, deterministic software-rendered `npm run test:electron` plus installed-executable Windows CI lifecycle |
| Render health | Starter world | Nonblank canvas, varied colors, one Phaser canvas | Browser + Electron smoke | Yes, `npm run test:browser`, deterministic `npm run test:electron`, production-default packaged smoke, and installed-executable Windows CI lifecycle |
| Avatar assets | Generated avatar catalog | No missing initial robot textures, saved forms render | E2E | Yes, `npm test` |
| Avatar creation preview | Open character creation at desktop and 390x844 widths, then resize | Selected avatar renders from loaded layered sprite art, Appearance precedes Traits on mobile, Start remains visible, and the scroll cue recalculates until the remaining fields are reached | Browser E2E + visual | Yes, `npm run test:browser`, plus 30-state Electron screenshot review |
| Camera controls | Pan and center actions | Finite scroll/focus values and follow reset | E2E | Yes, `npm test` |
| Initial camera framing | Start gameplay at desktop and 390x844 widths | Starter home is the dominant desktop subject while mobile retains full-room context | Browser E2E + visual | Yes, `npm run test:browser`, plus screenshot review |
| Object market | Open Market and buy offer | Offer list renders, inventory increases | E2E | Yes, `npm test` |
| Collections | Open Collections and claim ready set | Ready claims work and panel refreshes | E2E | Yes, `npm test` |
| Home goals | Open Goals | Active goal cards and disabled incomplete claims render | E2E | Yes, `npm test` |
| Build renovate tab | Open Build, switch Renovate | Layout, resize, furnish, floor, and lot controls render | E2E | Yes, `npm test` |
| Family assignments | Seed household members | Routine buttons render and assignment state updates | E2E | Yes, `npm test` |
| Mobile HUD and resize continuity | Resize live play from desktop to 390×844 | Actions, needs, campaign objective, persistent menu controls, and all starter furniture remain visible/synchronized with no horizontal overflow | Browser E2E + visual | Yes, `npm run test:browser`, plus screenshot review |
| Activity feedback | Start activity from mobile Activities panel | Panel closes and HUD immediately shows active movement | Visual | Manual screenshot |
| Offline engine boot | Block all external network requests and launch Electron | Main menu and gameplay render from local dependencies | E2E | Yes, `npm test` |
| Campaign migration | Load a legacy state without `campaign` | Campaign defaults are added without changing existing progression | Unit | Yes, `npm test` |
| Campaign progression | Complete the active chapter condition | Chapter completes once, reward is granted once, next objective appears | Unit + E2E | Yes, `npm test` |
| Pause shell | Pause while running or already paused | Underlying UI becomes inert, focus moves to Resume, and close restores focus and the exact prior speed including zero | Browser E2E | Yes, `npm run test:browser` |
| Sim Makeover modal | Open and close the in-game avatar editor | Simulation pauses, underlying UI is inert, dialog label/close semantics are exposed, and speed plus trigger focus are restored | Browser E2E | Yes, `npm run test:browser` |
| Settings persistence | Set volume, mute, scanlines, reduced motion, high contrast, and graphics quality | Preferences apply immediately; localStorage, body classes, and controls rehydrate after reload/load | Packaged E2E | Yes, production-default and `--software-rendering` `npm run verify:artifact-runtime -- <candidate-dir>` |
| Retro HUD | Desktop and 390x844 viewport | Campaign objective, needs, and primary commands remain readable without central obstruction | Browser E2E + visual | Yes, `npm run test:browser`, plus screenshot review |
| Storybook presentation | Menu, creator, gameplay, every major side panel, event, pause, notifications, placement, interaction wheel, and runtime recovery at desktop/mobile widths | Teal/ivory/gold/coral semantics remain consistent, the world stays centered and saturated, controls do not clip, and transient layers do not collide | Browser + Electron visual smoke | Yes, `npm run test:browser` plus the reviewed 30-state `npm run test:visual` matrix |
| World asset variety | Load a fresh starter home and enumerate furniture mappings | 125+ embedded world textures, 30+ distinct furniture mappings, 28+ generated household categories, 32 distinct 256x256 custom sprites, and no missing textures | Resource + E2E + visual | Yes, `npm test`, plus desktop/mobile gameplay screenshots |
| Built-in map integrity | Validate every default map | Known room/furniture types, valid room references, in-bounds footprints, and no furniture overlaps | Unit | Yes, `npm test` |
| Career contract | Enumerate every career | Eight careers, five progression levels, valid workplace/action references, and an available activity target | Unit | Yes, `npm test` |
| Save corruption recovery | Load malformed index, legacy, slot, nested map entries, and import payloads | Menu remains usable; unsafe keys, oversized payloads, malformed nested records, and invalid data are rejected; recoverable legacy data is preserved | Unit | Yes, `npm run test:pure` |
| Atomic active-slot save | Fail either the slot or index storage write while updating an existing slot | Both values roll back to their prior snapshots and the failure is reported | Unit | Yes, `npm run test:pure` |
| Lifecycle persistence | Hide or leave the gameplay page with an active slot | A single bound lifecycle handler attempts to persist the active slot without duplicate listeners | Unit + static contract | Yes, `npm run test:pure` |
| Save resource bounds | Import payloads exceeding byte, depth, node, string, map, floor, room, furniture, or broken-ID limits | Import is rejected before migration or merge work can exhaust renderer resources | Unit | Yes, `npm run test:pure` |
| Persistent collection schema | Load or import null/malformed pet, action-queue, moodlet, inventory-object, family-member, and related persistent arrays | Candidate is rejected before derived systems or the renderer can dereference malformed entries | Unit | Yes, `npm run test:pure` |
| Travel and path lifecycle | Complete portal/subway travel and cancel an in-flight path request | Map transition completes, travel achievement records, and stale callbacks cannot resume movement | Unit | Yes, `npm test` |
| Keyboard shortcuts | Use Space, 1-3, C/Home, B, J, Q, L, WASD, arrows, and Escape | Pause/speed, camera, build, journal, autonomy, graphics, and pause-shell focus/isolation perform the documented action | Packaged E2E | Yes, both packaged runtime rendering modes |
| Foundation services | Seed randomness, subscribe/unsubscribe signals, enumerate asset groups | Random sequences repeat, signals detach, and all stable asset groups exist | Unit | Yes, `npm run test:pure` |
| JavaScript type gate | Run the configured incremental JavaScript analysis | TypeScript 7.0.2 `checkJs` completes with `strictNullChecks` enabled and no diagnostics | Static analysis | Yes, `npm run typecheck` |
| Generated asset consistency | Compare checked-in generated catalogs and their source files in read-only mode | 82 world entries and 1,000 avatar entries match 1,000 avatar PNG files; generated text comparison is CRLF-normalized and no files are written | Resource/integration | Yes, `npm run verify:generated` |
| Performance posture | Review current renderer and save-growth audit against escalation thresholds | No P1/P2 finding; defer telemetry/spatial buckets unless sustained frame-time p95 exceeds 33 ms, repeated stalls exceed 100 ms, or saves near thousands of objects | Audit | Yes, documented review |
| Save schema v2 | Load a versionless/v1 save with unknown fields | Ordered migration reaches v2, preserves unknown fields, and remains idempotent | Unit | Yes, `npm run test:pure` |
| Trait contract | Exercise Neat, Lazy, Creative, Athletic, Charming, and Glutton effects | Configured decay, duration, cost, nap, and relationship multipliers affect simulation | Unit | Yes, `npm run test:pure` |
| Verification isolation | Run pure and Electron scopes independently | Each scope reports its own result; Electron integration uses deterministic Canvas/software rendering, supports opt-in `SIMLIFE_ELECTRON_GPU=1`, owns an isolated process/profile lifecycle, and leaves no project Electron process or temporary profile behind | Integration | Yes, `npm run test:pure`, `npm run test:electron` |
| Runtime recovery states | Trigger renderer loss after gameplay begins | Accessible alert dialog blocks stacked input, pauses time, closes other surfaces, and offers a persisted retry | Browser E2E + visual | Yes, `npm run test:browser` |
| Workspace structure | Run from a fresh checkout | Only approved root files/directories are tracked, SOP tooling is lockfile-pinned, and artifacts contain only the placeholder | Integration | Yes, `npm run verify:structure` |
| Asset provenance | Validate licensed packs, archives, generators, and unresolved records | Checksums and declared paths match; source-only material is excluded from packaging | Resource | Yes, `npm run verify:assets` |
| Static package contract | Validate Electron file globs and shipped HTML/CSS | Required runtime paths are packaged, raw source packs are excluded, and no remote runtime/style dependency exists | Integration | Yes, `npm run build:verify` |
| Built package contents | Inspect a generated `app.asar` and NSIS candidate | Required runtime files exist; tests/tools/docs/Python/source-only packs/previews/archives are absent; installer SHA-256 is recorded | Package integration | Yes, `npm run verify:artifact -- <candidate-dir>` |
| Packaged offline journey | Launch generated unpacked Electron with an isolated profile and external host resolution blocked | Menu → create → render → controls/settings → save → reload → Load World succeeds with rehydrated preferences, all assets/furniture, one canvas, and no external requests or runtime errors | Packaged E2E | Yes, `npm run verify:artifact-runtime -- <candidate-dir>`; explicit `--software-rendering` repeats the full gate while CI retains the default path |
| Installed NSIS lifecycle | Clean GitHub `windows-latest` runner and one unsigned candidate installer | Refuse pre-existing footprints; silently install for the current user; verify version, installed executable/uninstaller, desktop and Start Menu shortcuts, and HKCU uninstall entry; run the full offline save/load journey from the installed executable; silently uninstall; verify all recorded install footprints are removed | Installer E2E + rollback cleanup | Yes, `npm run verify:installer-lifecycle -- <candidate-dir>` in Windows CI; `--dry-run` and pure helper checks are non-mutating |
| Load-game menu journey | Seed a valid local save, open Load Game, select the first slot | Existing save enters gameplay with the expected character/world state | E2E | Yes, `npm run test:electron` and clean-runner installed-executable lifecycle |
| Real browser gameplay smoke | Exercise empty Load Game, creator, live play, event, pause, side panel, mobile HUD, and renderer recovery in Chrome | Renderer starts, time advances, progression rules hold, focus/inert contracts hold, mobile content remains visible, and recovery blocks stacked input | Browser E2E | Yes, `npm run test:browser` |
| Full quality composition | Run the repository test command | Structure, provenance, generated assets, syntax, typecheck, pure, and Electron gates all pass in sequence | Integration | Yes, `npm test` |
